using System;
namespace VariableCheckApp
{
    class Program
    {
        static void Main(string[] args)
        {
            // var uberflu? = 0; // нельзя использовать симоволы (?).
               int _Identifier = 0;  // можно использовать при условии инициализации.
               int \u006fIdentifier =0; // можно использовать при условии инициализации, старый стиль обьявления переменной.
            //  var &myVar = 0;  // нельзя использовать симоволы (&).
               int myVariab1le = 0; // можно использовать при условии инициализации, старый стиль обьявления переменной. 
        }
    }
}
